<div class="col-md-6">
    <div class="card flex-md-row mb-4 box-shadow h-md-250">
      <div class="card-body d-flex flex-column align-items-start">
        <strong class="d-inline-block mb-2 text-primary">World</strong>
        <h3 class="mb-0">
          <a class="text-dark" href="#"><%= blog.title %></a>
        </h3>
        <div class="mb-1 text-muted"><%= distance_of_time_in_words(blog.created_at, Time.now) %> agot</div>
        <p class="card-text mb-auto"><%= blog.body %></p>
        <a href="#">Continue reading</a>
      </div>
      <img class="card-img-right flex-auto d-none d-md-block" data-src="holder.js/200x250?theme=thumb" alt="Card image cap">
    </div>
    <div><%= link_to 'Show', blog %></div>
    <div><%= link_to 'Edit', edit_blog_path(blog) if logged_in?(:site_admin)%></div>
    <div><%= link_to 'Destroy', blog, method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin)%></div>
    <div><%= link_to blog.status, toogle_status_blog_path(blog), method: :post%></div>
  </div>